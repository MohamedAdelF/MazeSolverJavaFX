# تشغيل التطبيق على Windows

## المتطلبات

1. **Java 11 أو أحدث** (يفضل Java 17+)
   - تحميل من: https://adoptium.net/ أو https://www.oracle.com/java/technologies/downloads/
   - تأكد من إضافة Java إلى PATH

2. **Gradle** (اختياري - يمكن استخدام Maven أيضاً)
   - تحميل من: https://gradle.org/install/
   - أو استخدم Gradle Wrapper المرفق

## طريقة التشغيل

### الطريقة 1: باستخدام Gradle (الأسهل)

```cmd
cd javafx
gradle clean run
```

أو إذا كان Gradle غير مثبت، استخدم Gradle Wrapper:

```cmd
cd javafx
gradlew.bat clean run
```

### الطريقة 2: باستخدام Maven

```cmd
cd javafx
mvn clean javafx:run
```

### الطريقة 3: تجميع وتشغيل يدوياً

```cmd
cd javafx

# التجميع
gradle clean build

# التشغيل
java --module-path "C:\path\to\javafx-sdk-17\lib" ^
     --add-modules javafx.controls,javafx.fxml ^
     -cp build\classes\java\main ^
     com.mazesolver.Main
```

## ملاحظات مهمة

✅ **JavaFX يعمل بشكل ممتاز على Windows** - لا توجد مشاكل مثل macOS
✅ **لا حاجة لإعدادات خاصة** - يعمل مباشرة
✅ **الأداء أفضل** - JavaFX محسّن لـ Windows

## استكشاف الأخطاء

إذا واجهت مشكلة `ClassNotFoundException`:
1. تأكد من تثبيت Java 11+ بشكل صحيح
2. تحقق من أن Java في PATH: `java -version`
3. إذا استمرت المشكلة، حمّل JavaFX SDK يدوياً من https://openjfx.io/

## الميزات المتاحة

- ✅ اختيار نوع البنية (Stack/Queue/Linked List)
- ✅ لوحة تحكم كاملة
- ✅ إحصائيات مباشرة
- ✅ تصور البنيات البيانات
- ✅ متاهات جاهزة
- ✅ تحرير المتاهة

**التطبيق جاهز ويعمل بشكل ممتاز على Windows! 🎉**

