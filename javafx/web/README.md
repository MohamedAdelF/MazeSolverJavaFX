# نشر التطبيق أونلاين

## 🚀 طريقة سريعة

### 1. إنشاء ملف JAR:

```bash
cd javafx
gradle prepareWebDeployment
```

### 2. رفع الملفات:

**على GitHub Pages:**
1. ارفع محتويات مجلد `web/` إلى GitHub Pages
2. أو استخدم أي خادم ويب

**الملفات المطلوبة:**
- `maze-solver-1.0.jar` - ملف التطبيق
- `maze-solver.jnlp` - ملف Java Web Start
- `index.html` - صفحة الويب

### 3. الوصول:

افتح `index.html` في المتصفح أو استخدم GitHub Pages URL.

---

## ⚠️ ملاحظات

- **Java Web Start** يحتاج Java مثبت في المتصفح
- بعض المتصفحات الحديثة لا تدعمه
- في هذه الحالة، حمّل JAR وشغّله محلياً

---

## 🔄 تحديث JNLP

إذا غيرت موقع الملفات، عدّل `maze-solver.jnlp`:
- `codebase`: URL الأساسي
- `href`: اسم ملف JNLP
- `jar href`: اسم ملف JAR

